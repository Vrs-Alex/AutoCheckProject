# Icons

SVG icons are sourced from the public Lucide icon set:

https://lucide.dev

They are kept as local assets so the Flutter app can run without a runtime icon-pack dependency. The app renders them through `flutter_svg` in `TechIcon`.
